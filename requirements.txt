# ============================================================
# NOM-HRMS-FGA — зависимости среды выполнения
# ============================================================
# Мягкий пиннинг (минимальные нижние границы). Версии подобраны
# так, чтобы гарантировать наличие используемого API при широкой
# совместимости. Список проверен по фактическим import в src/,
# tests/, tools/ и src/sim/.
#
# Системная зависимость (НЕ ставится через pip):
#   * tkinter — часть стандартной библиотеки CPython, но в ряде
#     дистрибутивов Linux поставляется отдельным пакетом
#     (например, apt install python3-tk). Требуется только для
#     графического интерфейса (src/app.py, src/ui, src/structures).
#
# Требуемая версия Python: >= 3.10
#   (в коде используется синтаксис union из PEP 604, напр. `float | None`).
# ------------------------------------------------------------

# --- Численные вычисления и работа с таблицами ---
numpy>=1.24
pandas>=2.0

# --- Визуализация ---
matplotlib>=3.7

# --- Химинформатика: разбор формул, сборка и отрисовка структур ---
rdkit>=2023.9
# Примечание: если пакет `rdkit` недоступен для вашей платформы,
# используйте альтернативу `rdkit-pypi>=2022.9`.

# --- Отрисовка структур в GUI (миниатюры/экспорт PNG) ---
Pillow>=9.0

# ------------------------------------------------------------
# Опциональные зависимости (нужны только для отдельных сценариев)
# ------------------------------------------------------------
# --- Тестирование ---
pytest>=7.0
# Генерация синтетических тест-сетов из PubChem
# (src/sim/search_pubchem_nom_like_cids.py):
#   pubchempy>=1.0.4
#
# Встроенный вывод структур в средах Jupyter/IPython
# (опциональный импорт в src/core/rdkit_bridge.py):
#   ipython>=8.0
#
# Усреднение ThermoRAW-файлов (только Windows, src/core/io/raw_thermo_adapter.py):
#   pythonnet>=3.0
#   + RawFileReader DLLs в thermo/ (проприетарные, ThermoFisher)
#
# Конвертация mzML → CSV (src/core/mzml_bridge.py):
pymzml>=2.5
zstandard>=0.22
