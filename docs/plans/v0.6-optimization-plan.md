# План v0.6 — финальная оптимизация и UX

> **Создан:** 2026-08-03 | **Статус:** 🔴 план

## Сводка

После полной замены GPL-кода на MPL-2.0 (nomspectra, pymsfilereader, comtypes)
остались задачи по производительности, UX и тестированию на реальных данных.

---

## Задачи

| # | GitHub Issue | Задача | Приоритет | Статус |
|---|-------------|--------|:---:|:---:|
| 1 | `perf/pipeline-progress` | Подключить `progress_callback` в pipeline — показывать % выполнения каждого этапа в GUI | 🟠 | [ ] |
| 2 | `perf/optimize-find-series` | `find_series`: `.iterrows()` → `.itertuples()`, кэш `parse_formula` | 🟠 | [ ] |
| 3 | `perf/real-data-benchmark` | Замеры времени на реальных образцах (480k+ пиков) — определить, нужен ли C-генератор | 🟡 | [ ] |
| 4 | `feat/c-generator` | Переписать `_generate_candidate_formulas` на C (если замеры покажут необходимость) | ⚪ | [ ] |

---

## Готовые тексты для GitHub Issues

### #1: `perf/pipeline-progress` — Визуальный прогресс этапов пайплайна в GUI

**Описание:** Сейчас прогресс-бар в GUI крутится в indeterminate-режиме. Пользователь не видит, на каком этапе пайплайн и сколько осталось. Инфраструктура готова: `progress_callback` добавлен в сигнатуру `run_pipeline`, очередь сообщений поддерживает тип `"progress"`, прогресс-бар переведён в determinate-режим.

**Нужно:**
- [ ] Добавить вызовы `progress_callback("Загрузка", 20)` / `("Шумоподавление", 40)` / `("Формулы", 60)` / `("Серии", 80)` / `("Готово", 100)` в ключевых точках `run_pipeline`
- [ ] Подключить `_progress` в `_run_worker` (GUI-сторона уже написана, раскомментировать)

**Файлы:** `src/core/pipeline.py`, `src/app.py`

---

### #2: `perf/optimize-find-series` — Оптимизация find_series

**Описание:** `find_series` использует `.iterrows()` (pandas-антипаттерн) и вызывает `parse_formula` для каждой строки без кэша. После оптимизации `assign_formulas` (ленивые строки, `.itertuples()`) это осталось последним узким местом с `.iterrows()`.

**Нужно:**
- [ ] Заменить `.iterrows()` на `.itertuples()` в `find_series` (строка ~916)
- [ ] Добавить кэш `parse_formula` (словарь `{formula_str: counts}`)
- [ ] Убрать `np.asarray` из `_find_peak` — caller уже передаёт `.values`

**Ожидаемый эффект:** −0.3s на 5 тестовых сетов

**Файлы:** `src/core/spectrum_ops.py`

---

### #3: `perf/real-data-benchmark` — Бенчмарк на реальных образцах

**Описание:** Все замеры производительности делались на синтетических тестовых наборах (30 сигналов, ~500 пиков). Реальные образцы — 480k+ пиков. Нужно прогнать полный пайплайн на `.raw`-файлах и замерить время каждого этапа.

**Нужно:**
- [ ] Запустить `run_pipeline` на реальном `.raw` (SRFA_ESI_480k)
- [ ] Снять тайминги через `PipelineTimings` (load, denoise, assign, series, build_table)
- [ ] Сравнить с синтетическими наборами
- [ ] Принять решение: достаточно ли Python-оптимизаций или нужен C-генератор

**Файлы:** `src/core/pipeline.py` (таймер уже есть), `src/core/timer.py`

---

### #4: `feat/c-generator` — C-генератор брутто-формул

**Описание:** Если бенчмарк (#3) покажет, что генератор формул остаётся узким местом (>2s на реальных данных), переписать `_generate_candidate_formulas` на C (CPython extension).

**API:**
```c
struct candidate { int c, h, o, n; double mass; };
candidate* generate_formulas(double mass_min, double mass_max,
                              int c_min, int c_max,
                              int h_min, int h_max,
                              int o_min, int o_max,
                              int n_min, int n_max,
                              int* out_count);
```

**Ожидаемое ускорение:** ×40–100 относительно Python-версии

**Риски:** усложнение сборки, дублирование хим. правил (Lewis, Senior)

**Блокируется:** #3 (нужны замеры)

**Файлы:** новый `src/core/_generator.c` + `setup.py` / `pyproject.toml`

---

## Порядок выполнения

```
#1 (progress) → #2 (find_series) → #3 (benchmark) → #4 (C-generator, опционально)
```
