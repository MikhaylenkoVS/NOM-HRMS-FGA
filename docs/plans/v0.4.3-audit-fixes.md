# План закрытия milestone v0.4.3 — NOM-HRMS-FGA

> **Создан:** 2026-07-12 | **Статус:** ✅ завершён | **Milestone:** [v0.4.3 — Багфиксы и доработки](https://github.com/MikhaylenkoVS/NOM-HRMS-FGA/milestone/4) | **Закрыт:** v0.5.0 (13.07.2026)

## Сводка

8 issues по итогам полного аудита кода 12.07.2026. Проверено 26 файлов, ~9500 строк.

---

## 🐛 Баги (приоритет: высокий)

### #74 — `_import_folder` fallback ищет только CSV, игнорируя RAW
**Файл:** `src/app.py:1368` | **Сложность:** 1 строка
- [x] Заменить `for f in csv_files:` → `for f in all_files:` (исправлено: all_files = csv_files + raw_files)

### #78 — `_auto_plot_hist` молча глотает исключения
**Файл:** `src/app.py:1630` → `src/ui/_plots.py` | **Сложность:** 3 строки
- [x] Добавить `messagebox.showwarning(...)` в блоке `except` перед логированием (уже есть)

---

## 📝 Документация (приоритет: средний)

### #75 — `src/README.md` упоминает несуществующие артефакты
**Файл:** `src/README.md:39` | **Сложность:** 2 строки
- [x] Удалить строки про `result_table.csv` и `van_krevelen.png` (уже удалены)

### #80 — `generate_test_sets.py` docstring — старый формат конфига
**Файл:** `src/simulations/generate_test_sets.py:314` → `src/sim/generate_test_sets.py` | **Сложность:** 2 строки
- [x] Заменить `config["derivatization"]` и `config["noise"]` на `pipeline.json → test_mode` (уже исправлено — ссылок на старый формат нет)

### #81 — `src/README.md` ссылается на несуществующий `core/README.txt`
**Файл:** `src/README.md:28` | **Сложность:** 1 строка
- [x] Убрать упоминание `core/README.txt` (уже удалено)

---

## 🧹 Чистота кода (приоритет: низкий)

### #76 — `fragment_combinations.py` — отладочные print()
**Файл:** `src/core/fragment_combinations.py` | **Сложность:** 10 строк
- [x] Заменить ~10 вызовов `print()` на `logging.info()` или убрать (0 вызовов print, уже чисто)

---

## 🧪 Тесты (приоритет: средний)

### #77 — Отсутствуют unit-тесты для 7 модулей
**Файлы:** `atoms.py`, `fragments.py`, `fragment_combinations.py`, `molecule.py`, `pipeline.py`, `raw_bridge.py`, `rdkit_bridge.py`
- [x] `test_atoms.py` (существует) — тесты на ELEMENT_DATA, Hybridization
- [x] `test_fragments.py` (существует) — валидность FRAGMENT_LIBRARY, FUNCTIONAL_GROUPS
- [x] `test_fragment_combinations.py` (существует) — базовые комбинации
- [x] `test_molecule.py` (существует) — parse_formula, IHD, граф
- [x] `test_pipeline.py` (существует) — smoke-тест run_pipeline
- [x] `test_raw_bridge.py` (существует) — мок pymsfilereader
- [x] `test_rdkit_bridge.py` (существует) — конвертация Molecule → RDKit

---

## ✨ Улучшения (приоритет: низкий, можно в v0.5)

### #79 — Индикация прогресса при длительных операциях
**Файлы:** `raw_bridge.py`, `structures/tab.py`, `app.py` | **Сложность:** ~20 строк
- [x] Передавать callback прогресса в `average_raw_to_csv` (progress_callback реализован)
- [x] Добавить `progressbar` в GUI для операций с RAW (progress bar используется)
- [x] Показывать статус при генерации структур (предзагрузка с логом)
